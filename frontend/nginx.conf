# Redirection HTTP (Port 80) et Port 5173 vers HTTPS officiel
server {
    listen 80;
    listen 5173;
    server_name _;

    # Autoriser le renouvellement automatique Let's Encrypt Certbot
    location /.well-known/acme-challenge/ {
        root /usr/share/nginx/html;
    }

    # Redirection 301 vers le domaine sécurisé avec certificat SSL Let's Encrypt
    location / {
        return 301 https://51.15.143.236.sslip.io$request_uri;
    }
}

# Serveur HTTPS SSL sécurisé (Port 443)
server {
    listen 443 ssl;
    server_name 51.15.143.236.sslip.io _;

    # Certificats SSL Let's Encrypt
    ssl_certificate /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;
    ssl_session_tickets off;

    # En-têtes de sécurité
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    client_max_body_size 50M;

    # Compression Gzip
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/json
        application/xml
        image/svg+xml;

    root /usr/share/nginx/html;
    index index.html;

    # Cache pour les assets statiques compilés par Vite
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # Proxy inverse vers le backend FastAPI pour toutes les routes API et médias
    location ~ ^/(auth|devices|sites|captures|contacts|scan|weather|alerts|tickets|static|docs|openapi.json|redoc|api) {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        # Support des WebSockets et streaming vidéo en direct
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Timeouts pour l'inférence IA, flux vidéo et uploads
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
        proxy_buffering off;
    }

    # Application React SPA (routage côté client)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Pages d'erreur
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
